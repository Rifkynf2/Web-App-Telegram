# =========================================================
# Vercel Environment Variables (Web App + API Gateway)
# Set via: Vercel Dashboard > Project > Settings > Environment Variables
# JANGAN commit file ini ke Git!
# =========================================================

# ========= Master Supabase (SERVER ONLY — API Gateway) =========
MASTER_SUPABASE_URL=https://ynorqtlefiskahvyykzl.supabase.co
MASTER_SUPABASE_SERVICE_KEY=your_master_service_role_key

# ========= Internal API Auth =========
# Shared secret antara API Gateway dan semua tenant bot
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
INTERNAL_API_SECRET=your_64_char_hex_secret_here

# ========= Admin Dashboard =========
# Secret untuk akses admin dashboard API (/api/admin/*)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_DASHBOARD_SECRET=your_admin_dashboard_secret_here
